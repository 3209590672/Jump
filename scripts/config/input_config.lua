-- ============================================================================
-- 输入键位配置
-- 所有按键集中在此，不散落在逻辑代码里
-- 后续 PC 改键或手机适配只需修改此文件
-- ============================================================================
return {
    -- 移动
    moveLeft = KEY_A,
    moveRight = KEY_D,

    -- 操作
    restart = KEY_R,
    fireMouse = MOUSEB_LEFT,
    slowMotionMouse = MOUSEB_RIGHT,

    -- 预留：后续可能用到的键位
    -- pause = KEY_ESCAPE,
    -- interact = KEY_E,
    -- weaponSwitch1 = KEY_1,
    -- weaponSwitch2 = KEY_2,
}
