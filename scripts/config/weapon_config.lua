-- ============================================================================
-- D1 武器配置：校准手枪（Calibrate Pistol）
-- 这是 D1 唯一的武器，用于教学玩家掌握反冲机制
-- 所有数值单位为逻辑像素
-- ============================================================================
return {
    calibratePistol = {
        recoilPower = 620,            -- 反冲冲量大小（px/s），决定单次开火飞多远
        cooldown = 0.32,              -- 开火冷却时间（秒），防止连点
        maxAirShots = 1,              -- 空中最大补枪次数，落地后恢复
        minAimLengthPixels = 12,      -- 瞄准向量最短长度（px），低于此值不开火（暂未使用）
        maxSpeedAfterRecoil = 1100,   -- 反冲后速度上限（px/s），防止叠加过快
    }
}
