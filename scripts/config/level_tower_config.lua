-- ============================================================================
-- 无限爬塔：最终关原型
-- 无终点，向上无限生成平台，死亡 = 掉出屏幕底部
-- 分数 = 到达的最大高度
--
-- 设计：
--   - 相机只往上走（跟随玩家最高点，不回落）
--   - 平台程序化生成，越高越难（更窄、间距更大、需要切换武器）
--   - 4 武器实时切换（当前原型先做手枪+霰弹枪）
--   - 掉落到相机底部以下 → 结算
-- ============================================================================
return {
    id = "tower",
    name = "无限爬塔",
    canvas = { w = 960, h = 99999 },  -- 无限高度
    spawn = { x = 480, y = 84 },
    fallY = -9999,                     -- 不用固定 fallY，用相机底部判定

    startWithGun = true,
    startWeapon = "pistol",
    enableWeaponSwitch = true,         -- 允许实时切换武器

    -- 塔模式特殊参数
    tower = {
        -- 平台生成
        startPlatformY = 60,           -- 起始平台高度
        generateAheadDistance = 800,   -- 提前生成距离（相机上方多远开始生成）
        removeBelow = 400,             -- 相机下方多远删除旧平台

        -- 难度曲线（根据高度变化）
        minGapY = 100,                 -- 最小垂直间距
        maxGapY = 220,                 -- 最大垂直间距
        minPlatformW = 80,             -- 最小平台宽度
        maxPlatformW = 200,            -- 最大平台宽度
        difficultyPerMeter = 0.001,    -- 每米增加的难度系数（0~1）

        -- 死亡判定
        deathBelowCamera = 120,        -- 掉到相机底部以下多少判定死亡
    },

    -- 不用预设平台，全部程序化生成
    platforms = {},
    triggers = {},
    dialogs = {},
    pickups = {},
}
