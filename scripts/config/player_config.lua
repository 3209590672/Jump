-- ============================================================================
-- 玩家移动参数配置
-- 所有数值单位为逻辑像素（画布 960×720）
-- 修改此文件可直接调整手感，无需改动逻辑代码
-- ============================================================================
return {
    -- 地面移动
    moveSpeed = 90,                   -- 地面最大水平速度（px/s）
    groundAcceleration = 1200,        -- 地面加速度（px/s²），值越大越"灵敏"
    groundFriction = 1600,            -- 松手后的地面摩擦力（px/s²），值越大越"刹车快"

    -- 空中控制
    airControlAcceleration = 180,     -- 空中水平微调加速度（px/s²），远弱于地面

    -- 重力（分离上升/下落手感）
    gravity = 1400,                   -- 上升阶段重力（px/s²），较小 → 飞得更远
    fallGravity = 1800,               -- 下落阶段重力（px/s²），较大 → 下落更"实"

    -- 速度上限
    maxFallSpeed = 1200,              -- 最大下落速度（px/s），防止过快穿透平台
    maxHorizontalSpeed = 900,         -- 最大水平速度（px/s），含反冲后的上限

    -- 平台碰撞宽容度
    groundSnapSpeed = 60,             -- 接近平台时的吸附缓冲速度（暂未使用）
    landingGraceWidth = 6,            -- 水平方向宽容量（px），让"差一点踩到"也能落地
    landingGraceHeight = 4,           -- 垂直方向宽容量（px），穿越判定的回溯容差

    -- 玩家碰撞体尺寸
    width = 32,                       -- 宽度（px）
    height = 48,                      -- 高度（px），position.y 为底部
}
